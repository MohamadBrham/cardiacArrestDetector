<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ps.wecare.cardiacarrestdetector">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".LanguageActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".GuideActivity"
            android:configChanges="locale" />
        <activity
            android:name=".Beloved.AddBeloved"
            android:label="@string/title_activity_beloved" />
        <activity
            android:name=".BluetoothConnectionActivity"
            android:configChanges="locale"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Beloved.BelovedList" />
        <activity android:name=".Beloved.UpdateBeloved" />
        <activity android:name=".Medications.AddMedication" />
        <activity android:name=".Medications.MedicationsList" />
        <activity
            android:name=".Medications.UpdateMedication"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".Doses.AddDose" />
        <activity android:name=".Doses.UpdateDose"></activity>
    </application>

</manifest>